<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>APPOINTMENT</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="scss/_utilities.scss.">
    <link rel="stylesheet" href="styles/styles.css">
</head>
<body>

    <table class="table">
        <thead class="table-light">
            <tr>
                <th>ID</th>
                <th>FIRSTNAME</th>
                <th>LASTNAME</th>
                <th>REASON FOR VISIT</th>
                <th>PHONE</th>
                <th>EMAIL</th>
                <th>DATE</th>
                <th>TIME</th>
                <th>ACTION</th>
             
            </tr>
        </thead>

        <tbody>
            <% if (data.length > 0) { %>
                <% for (let i = 0; i < data.length; i++) { %>
                    <tr>
                        <td><%= data[i].id %></td>
                        <td><%= data[i].firstname %></td>
                        <td><%= data[i].lastname %></td>
                        <td><%= data[i].reason_for_visit %></td>
                        <td><%= data[i].phone %></td>
                        <td><%= data[i].email %></td>
                        <td><%= new Date(data[i].date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                        <td><%= data[i].time %> </td>
                        <td>
                            <button class="btn btn-danger" onclick="location.href='/delete/<%= data[i].id %>'">Delete</button>
                        </td>
                        
                      
                    </tr>
                <% } %>
            <% } else { %>
                <tr>
                    <td colspan="3">No data available</td>
                </tr>
            <% } %>
            


           
        </tbody>
    </table>
    
</body>
</html>