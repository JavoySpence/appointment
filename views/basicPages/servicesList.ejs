<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ALL SERVICES</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="scss/_utilities.scss.">
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <button class="appointment-btn btn" onclick="window.location.href='/servicesInputs'">ADD</button>

    <table class="table">
        <thead class="table-light">
            <tr>
                <th>ID</th>
                <th>SERVICE</th>
                <th>ACTION</th>
            </tr>
        </thead>

        <tbody>
          <% if (data.length > 0) { %>
            <% for (let i = 0; i < data.length; i++) { %>
            <tr>
                <td><%= data[i].id %></td>
                <td><%= data[i].reason_for_visit %></td>
                <td>
                    <button class="appointment-btn btn" onclick="window.location.href='/deleteService/<%= data[i].id %>'">DELETE</button>
                  
                </td>
            </tr>
            <% } %>
            <% } else { %>
                <tr>
                    <td colspan="2">No data available</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>